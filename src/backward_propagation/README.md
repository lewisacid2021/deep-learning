# 误差反向传播

## 计算图

- 计算图将计算过程用图形表示出来。这里说的图形是数据结构图,通过多个节点和边表示(连接节点的直线称为“边”)。

- 特点是便于局部计算

- 从左到右（计算图出发点到结束点）为**正向传播**，从右到左为**反向传播**

## 反向传播

计算顺序：将信号 E 乘以节点的局部导数,然后将结果传递给下一个节点

原理：链式法则（复合函数求导，先求偏导再求导，计算导数乘积）

### 加法节点

- z=x+y 对x和y求偏导结果都为1 因此加法是原封不动的流入下个节点

### 乘法节点

- z=x*y 对x和y求偏导结果为y、x